profiles {
    apptainer {
        apptainer.enabled = true
        docker.enabled = false
        apptainer.autoMounts = true
        apptainer.cacheDir = params.apptainer_cache_dir ?: null
        process.containerOptions = params.apptainer_run_options ?: ''
    }
}
